export const metadata = {
  title: "IoT Hub - Device Network Monitor",
  description: "Real-time Matter + MQTT IoT device monitoring and control dashboard"
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <head>
        <meta name="viewport" content="width=device-width, initial-scale=1" />
      </head>
      <body>{children}</body>
    </html>
  );
}
